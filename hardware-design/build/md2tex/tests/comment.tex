
This is a normal paragraph, where \emph{inline format} is handled

% this line is a comment, where *inline format* should be ignored

we can \textbf{also} have a quote start % halfway through a line *like* this