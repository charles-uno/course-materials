beamer: true
---

### frame title!

no code in this one. it should not be fragile

### beamer frame title

this frame should be marked fragile because it contains a code block:
```python
print("hello world!")
```

### this is the frame title

frame contents here, which contain some `inline code` in the text

maybe even `twice` or `three` times