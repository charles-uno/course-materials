
variable \emph{string} with inline \verb|formatting| and \textbf{also a link} see \url{http://instagram.com}

and we can also do variable \emph{string} with inline \verb|formatting| and \textbf{also a link} see \url{http://instagram.com} within a line

it even works inside literal blocks, though we might want to change that in the future:
\begin{minted}{arm}
variable \emph{string} with inline \verb|formatting| and \textbf{also a link} see \url{http://instagram.com}
\end{minted}